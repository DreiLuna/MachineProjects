from cs1graphics import *
import random
import math

class Grass:
    def __init__(self) -> None:
        pass

    def placeGrassRandomly(self, amount, grassAmount):
        pass

    def makeClipping(self,i):
        for j in range(i):
                if j == 0:
                    pass
                else:
                     if locations[]
        clipping = Path(Point(x,y),Point(x,y+random.randint(5,10)))

    def placeGrass(self):
        clippings = random.randint(5,7)
        x = random.randint(200,800)
        y = random.randint(300,400)
        origin_grass = Path(Point(x,y),Point(x,y+random.randint(5,10)))
        locations= [[x,y]]
        for i in range(clippings):
            self.makeClipping(i)

